<style>
  @media (max-width: 767px) {
    .mobile-brand-font {
      font-size: 1.125rem !important; /* 18px, text-lg */
    }
  }
</style>
<div class="bg-[#F5EFE6] text-[#4A403A]">
  <!-- 헤더: index와 동일 구조 -->
  <section>
    <div class="container mx-auto max-w-none px-5 md:px-8 lg:px-12 xl:px-16">
      <div class="grid grid-cols-2 grid-rows-[auto] gap-px mobile-main-grid">
        <!-- Header Area -->
        <header class="col-span-2 grid grid-cols-2 items-center py-4 border-b border-dotted border-[#D6C6B4] mobile-header-grid font-serif">
          <div class="p-4 flex items-center mobile-header-brand">
            <%# 헤더 브랜드 클릭 시 홈(root_path)으로 이동하도록 링크 적용 %>
            <%= link_to root_path do %>
              <span class="text-xl font-bold tracking-[0.2em] mobile-brand-font">VERITA AI</span>
            <% end %>
          </div>
                  <nav class="p-4 flex items-center justify-end space-x-8 text-sm font-bold tracking-widest">
            <!-- 기존 메뉴: 일시 숨김 처리 (복구용으로 주석 보존) -->
            <!--
            <a href="#" class="hover:text-[#E86A33]">ABILITIES</a>
            <a href="#" class="hover:text-[#E86A33]">HOW IT WORKS</a>
            <a href="#" class="hover:text-[#E86A33]">STAY CONNECTED</a>
            -->
            <!-- Pricing 메뉴 추가 -->
                                <%# 현재 페이지가 pricing_path가 아닐 경우에만 링크 표시 %>
                                <% unless current_page?(pricing_path) %>
                                  <a href="/pricing" class="hidden md:inline-block hover:text-[#E86A33]">Pricing</a>
                                <% end %>
            <!-- 헤더 우측 CTA: 하단 버튼 스타일을 계승, 사이즈 축소 -->
            <!-- 버튼 좌우 여백 미세 조정: px-6 -> px-5 (세로는 원래 유지) -->
            <a href="/contact"
               class="rounded-lg bg-[#E86A33] px-5 py-2 text-sm font-semibold text-white shadow-sm hover:bg-orange-400 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-orange-500 transition duration-300 ease-in-out transform hover:scale-105 font-sans">
              무료 시작
            </a>
          </nav>
        </header>
      </div>
    </div>
  </section>

  <!-- 본문: 서비스 및 가격 정보 -->
  <section class="py-16">
    <div class="container mx-auto max-w-7xl px-5 md:px-8 lg:px-12 xl:px-16">
      <div class="text-center mb-12">
        <h1 class="text-4xl font-bold text-gray-900 mb-4">서비스 및 가격</h1>
        <p class="text-gray-600">Verita AI의 번역 서비스와 요금제를 한 눈에 확인하세요.</p>
      </div>

      <!-- 기준 가격 정보 -->
      <div class="mb-16 text-center">
        <h2 class="text-2xl font-bold text-gray-900 mb-8">기준 가격</h2>
        <div class="bg-white rounded-xl shadow-sm border border-[#D6C6B4] p-8 max-w-md mx-auto">
          <p class="text-lg text-gray-700 mb-4">1페이지 / 400단어</p>
          <div class="flex items-center justify-center gap-4">
            <span class="text-2xl font-bold text-gray-500" style="text-decoration: line-through; text-decoration-thickness: 2px; text-decoration-color: #6b7280;">25,000원</span>
            <span class="text-3xl font-extrabold text-[#E86A33]">9,900원</span>
          </div>
          <p class="text-sm text-[#E86A33] font-semibold mt-2">(할인 기간)</p>
        </div>
      </div>

      <!-- 가격 카드: @pricing_plans 기반 동적 렌더링 -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <% @pricing_plans.each do |plan| %>
          <% popular = plan[:popular] %>
          <div class="bg-white rounded-xl <%= popular ? 'shadow-md border border-[#E86A33] relative' : 'shadow-sm border border-[#D6C6B4]' %> p-6">
            <% if popular %>
              <span class="absolute -top-3 right-4 bg-[#E86A33] text-white text-xs font-bold px-2 py-1 rounded-full">추천</span>
            <% end %>
            <h3 class="text-xl font-bold mb-2"><%= plan[:name] %></h3>
            <p class="text-3xl font-extrabold mb-1">
              <%= plan[:price] %>
              <% if plan[:period].present? %>
                <span class="text-base font-medium text-gray-600"><%= plan[:period] %></span>
              <% end %>
            </p>
            <ul class="mt-4 space-y-2 text-sm text-gray-700">
              <% plan[:features].each do |f| %>
                <li><%= f %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
      </div>

      <!-- 통합 CTA 섹션 -->
      <div class="mt-16 mb-16 text-center">
        <div class="max-w-2xl mx-auto">
          <!-- 제목 위 여백을 소폭 추가하여 시각적 호흡 확보 (mt-2) -->
          <h3 class="text-2xl font-bold text-gray-900 mb-4 mt-2">각종 문의 사항이나 번역을 요청하세요</h3>
          <p class="text-lg text-gray-600 mb-8">
            전문적인 특허 번역 서비스를 지금 바로 시작하실 수 있습니다.<br>
            궁금한 점이 있으시면 언제든지 문의해 주세요.
          </p>
          <a href="/contact"
             class="inline-block rounded-lg bg-[#E86A33] px-8 py-4 text-lg font-semibold text-white shadow-sm hover:bg-orange-400 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-orange-500 transition duration-300 ease-in-out transform hover:scale-105">
            시작하기
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- 푸터: index의 하단 푸터 복제 -->
  </div>
